# CATPOS ↔ 팜포인트(토스) 가격표시기 연동 명세

CATPOS(POS)가 팜포인트 플러그인의 **고객 가격표시기 화면**에 카트 정보를 전달하기 위한 프로토콜입니다.

---

## 1. 통신 채널

기존 CATPOS ↔ 팜포인트 웹소켓 채널을 그대로 사용합니다.

| 항목 | 값 |
| --- | --- |
| 서버 | 팜포인트 플러그인(패드) |
| 프로토콜 | WebSocket |
| 포트 | `52391` |
| Path | `/` |
| 메시지 형식 | JSON |

기존 커맨드(`CONNECT` / `EARN_SINGLE_REQ` 등)와 **동일한 세션**에서 아래 신규 커맨드 2개를 주고받습니다. 별도 접속 필요 없습니다.

---

## 2. 메시지 봉투(envelope)

기존 CATPOS 프로토콜과 동일한 `{command, data}` JSON 형식입니다.

```json
{ "command": "...", "data": { ... } }
```

- CATPOS → 팜포인트로만 전송하며, **팜포인트는 응답을 보내지 않습니다.**
- 인식하지 못한 메시지는 무시됩니다(연결은 유지).

---

## 3. 커맨드

### 3-1. `CART_UPDATE` — 카트 스냅샷 전달

**POS → 팜포인트**. 상품 스캔·수정·삭제·할인 변경 등 카트 내용이 바뀔 때마다 전송합니다.

- **첫 수신 시** 대기화면 → 가격표시기 화면으로 전환
- **이후 수신 시** 화면 실시간 갱신
- 전체 스냅샷(증분 아님) — 매번 현재 카트 상태 전체를 보냅니다

```json
{
  "command": "CART_UPDATE",
  "data": {
    "items": [
      { "name": "타이레놀정 500mg", "unitPrice": 4800, "quantity": 2, "amount": 9600 },
      { "name": "판콜에이내복액",   "unitPrice": 1200, "quantity": 1, "amount": 1200 }
    ],
    "dispenseAmount": 8000,
    "subtotal": 6000,
    "extraAmount": 1000,
    "discountAmount": -1000,
    "expectedEarnPoint": 1500,
    "total": 15000
  }
}
```

#### 필드 명세

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| `items` | Array | ✅ | 상품 목록. 빈 배열 `[]` 허용(카트 비어 있음) |
| `items[].name` | string | ✅ | 상품명 |
| `items[].unitPrice` | number | ✅ | 단가(원). 데이터로는 수신하나 **화면에는 표시하지 않음** |
| `items[].quantity` | number | ✅ | 수량 |
| `items[].amount` | number | ✅ | 라인 합계(원) = `unitPrice × quantity` |
| `dispenseAmount` | number | ✅ | 조제금액(원) |
| `subtotal` | number | ✅ | 일반금액(원) |
| `extraAmount` | number | ✅ | 추가금액(원) |
| `discountAmount` | number | ✅ | 할인금액(원). **할인은 음수**로 전송 |
| `expectedEarnPoint` | number | ✅ | 적립 예상 포인트(P). 정보성. 합계에 포함되지 않음 |
| `total` | number | ✅ | 합계(원). **POS가 계산한 최종 청구액이 기준값** |

> 응답 없음. 팜포인트는 수신 즉시 화면 갱신만 합니다.

---

### 3-2. `CART_CLEAR` — 가격표시기 종료 (선택)

**POS → 팜포인트**. 결제 개시와 무관하게 **거래를 취소하고 대기화면으로 즉시 복귀**시키고 싶을 때 사용합니다.

```json
{ "command": "CART_CLEAR" }
```

- `data` 필드 없음
- 응답 없음
- 수신 즉시 대기화면 복귀

> **결제 개시 시에는 별도로 보내지 않아도 됩니다.**
> 팜포인트는 결제 앱이 웹뷰를 덮는 순간(`visibilitychange`) 을 감지해 **자동으로 대기화면으로 복귀하고 카트를 초기화**합니다.
> 키오스크 환경이라 화면 잠금·앱 전환 등 다른 사유로는 트리거되지 않아, 이 자동 감지가 결제 시나리오를 정확히 커버합니다.
>
> `CART_CLEAR`가 필요한 경우는 **결제 없이 거래를 취소**하는 시나리오입니다(예: 사용자가 카트 취소 후 처음부터 다시).
> 또는 `CART_UPDATE`에 `items: []` (빈 배열) 을 보내도 같은 효과 — 팜포인트는 빈 카트를 받으면 대기화면으로 복귀합니다.

---

## 4. 데이터 흐름

```
[대기화면]
    ↓  (상품 첫 스캔)
CATPOS ──▶ CART_UPDATE           팜포인트: 가격표시기 화면 전환
    ↓  (상품 추가/수정/삭제)
CATPOS ──▶ CART_UPDATE           팜포인트: 화면 실시간 갱신
CATPOS ──▶ CART_UPDATE
CATPOS ──▶ CART_UPDATE
    ↓  (사용자가 "결제하기" 누름)
CATPOS ──▶ (결제 요청 시작)      [토스 결제 화면이 웹뷰를 덮음]
                                 팜포인트: 웹뷰 hidden 감지 → 자동으로 대기화면 복귀 + 카트 초기화
    ↓
[결제 완료 → 팜포인트 웹뷰 다시 보임: 대기화면]

──────────────────────────────────────────
결제 없이 거래 취소하는 경우 (선택)
CATPOS ──▶ CART_CLEAR            팜포인트: 즉시 대기화면 복귀
```

---

## 5. 개발 시 유의사항

1. **매번 전체 스냅샷 전송** — `CART_UPDATE`는 카트 전체를 다시 보내는 방식입니다. 증분/변경분만 보내지 마세요.
2. **`total`은 POS가 계산한 최종값** — 팜포인트는 `total`을 그대로 화면에 표시하며, `dispenseAmount + subtotal + extraAmount + discountAmount`와 검산하지 않습니다.
3. **할인은 음수** — `discountAmount`는 항상 음수 또는 0.
4. **`expectedEarnPoint`는 정보성** — 화면에 "적립예상"으로 표시되며 `total`에 더해지지 않습니다.
5. **`CART_CLEAR`는 선택** — 결제 개시 시에는 팜포인트가 자동으로 대기화면 복귀합니다. `CART_CLEAR`는 결제 없이 거래를 취소할 때(예: 사용자가 카트 취소 후 처음부터 다시)만 필요합니다. 같은 목적으로 `CART_UPDATE`에 `items: []` 를 보내도 됩니다.
6. **인식 불가 메시지는 무시** — 응답도 오지 않으므로 CATPOS 측 응답 대기·재시도 로직은 필요 없습니다.
7. **문자 인코딩** — UTF-8.

---

## 6. 화면 참고 (팜포인트 렌더 결과)

`CART_UPDATE` 데이터가 화면에 매핑되는 방식(POS 개발과 무관, 참고용):

- **상품 리스트**: 상품명 · 수량 · 금액 (3열)
- **금액 요약**: 조제금액 / 일반금액 / 추가금액 (파랑) · 할인금액 (빨강) · 적립예상 (파랑)
- **하단**: **합계** — `total` 값
- CTA 버튼 없음(표시 전용)
