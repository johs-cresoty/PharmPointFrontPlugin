# TRM 005 / 999 명세 (PharmPoint Toss 플러그인)

단말기(TRM)와 팜포인트 웹뷰 간 시리얼 통신에 추가된 커맨드 2종의 구현 명세입니다.

- **005 / 006** — 바코드 표시 요청 / 응답
- **999** — 화면 미노출 요청 (응답 전문 없음)

기존 001~004 / 010 과 동일한 봉투(envelope) 구조를 그대로 사용합니다. 봉투 구조 자체는
`terminal-serial-protocol-spec.md` 를 참고하세요.

---

## 1. 공통 봉투 구조

```
STX │ "XX"  │ 길이(4) │ 플래그(3) │ CMD(3) │ FS 필드 FS 필드 … │ ETX │ LRC
02  │ 58 58 │ ASCII×4 │ TRM / PAD │ 005 등 │ 1C…               │ 03  │ --
```

| 구간 | 크기 | 값 |
| --- | --- | --- |
| STX | 1 | `0x02` |
| 마커 | 2 | `"XX"` = `0x58 0x58` |
| 길이 | 4 | **STX~ETX 총 바이트 수** (길이 필드 4바이트 포함, **LRC 제외**). ASCII 4자리 zero-pad |
| 플래그 | 3 | 단말기→웹뷰 `"TRM"` / 웹뷰→단말기 `"PAD"` |
| CMD | 3 | `"005"` / `"006"` / `"999"` |
| 필드부 | 가변 | 각 필드 앞에 FS(`0x1C`) |
| ETX | 1 | `0x03` |
| LRC | 1 | STX 다음 바이트부터 ETX 까지 XOR 누적 후 `0x20` 을 OR |

**수신 전문은 모두 ACK 자동 회신**됩니다. `0x06 0x06 0x06` 3바이트이며, 커맨드 종류와 무관하게
프레임 파싱이 성공하면 게이트웨이가 자동으로 보냅니다.

---

## 2. CMD 005 — 바코드 표시 요청 (TRM → PAD)

### 2-1. 필드 구성

```
"005" + FS + 바코드종류(1) + FS + timeout(2) + FS + 문구(가변) + FS + 길이(2) + 바코드데이터
```

| No | 필드명 | 크기 | 내용 |
| --- | --- | --- | --- |
| 1 | CMD | 3 | `005` |
| 2 | 바코드 종류 | 1 | `1` = 1차원(EAN-13) / `2` = 2차원(QR) |
| 3 | timeout | 2 | `00`~`99` 초. `00` 이면 무한 대기(자동 종료 없음) |
| 4 | 문구 | 가변 | 바코드와 함께 표시할 안내 문구. **EUC-KR** |
| 5 | 바코드 데이터 길이 | 2 | **packed BCD**. 아래 2-2 참고 |
| 6 | 바코드 데이터 | 길이만큼 | 인코딩할 원본 문자열 (ASCII) |

### 2-2. 길이 필드는 BCD 입니다

2바이트 packed BCD 이며, 각 바이트의 상위/하위 4비트가 각각 십진수 한 자리를 나타냅니다.
2바이트 = 4자리 = `0000` ~ `9999` 를 표현합니다.

```
0x03 0x45  →  "03" "45"  →  0345  →  345 바이트
0x00 0x13  →  "00" "13"  →  0013  →   13 바이트
```

> **주의**: 빅엔디안 정수가 아닙니다. `0x0345` 를 정수로 읽으면 837 이 되어 값이 달라집니다.

명세상 "길이에 345바이트라고 되어 있으면 데이터를 345바이트까지 잘라서 사용" 이므로,
실제 남은 바이트가 더 많아도 길이 필드 값만큼만 사용합니다. 남은 바이트가 더 적으면
있는 만큼만 사용하고 경고 로그를 남깁니다.

### 2-3. 파싱 시 주의 — fields 배열을 쓸 수 없습니다

001~004 는 `TerminalCodec.parse()` 가 만들어주는 `fields` 배열을 그대로 씁니다. 하지만
**005 는 원본 바이트에서 직접 읽어야 합니다.**

`parse()` 는 필드부를 FS 로 자른 뒤 **전부 EUC-KR 디코딩**하는데, 길이 필드는 BCD 바이너리라
디코딩 과정에서 값이 손상됩니다. 예를 들어 길이 바이트가 `0x81` 이면 EUC-KR 선행바이트로
인식되어 뒤 바이트까지 함께 삼켜버립니다.

구현에서는 `parseBarcodeDisplay(raw)` 가 봉투 위치만 공용 로직으로 찾고, 필드부는
오프셋 기반으로 직접 읽습니다.

- 문구 필드는 EUC-KR 한글이 `0x1C` 를 포함하지 않으므로 FS 스캔이 안전합니다
- 길이 2바이트는 BCD 유효성(각 4비트 0~9)을 검사하고, 아니면 전문을 버립니다

### 2-4. 검증된 실제 프레임 (2차원)

389바이트, 길이 헤더 `0388`, 데이터 345바이트.

| 오프셋 | 바이트 | 의미 |
| --- | --- | --- |
| 0 | `02` | STX |
| 1–2 | `58 58` | `"XX"` |
| 3–6 | `30 33 38 38` | `"0388"` |
| 7–9 | `54 52 4D` | `"TRM"` |
| 10–12 | `30 30 35` | `"005"` |
| 13 | `1C` | FS |
| 14 | `32` | 종류 `"2"` = 2차원 |
| 15 | `1C` | FS |
| 16–17 | `39 39` | timeout `"99"` |
| 18 | `1C` | FS |
| 19–38 | `B9 D9 C4 DA …` | 문구 (EUC-KR) `"바코드를 읽어주세요 "` 20바이트 |
| 39 | `1C` | FS |
| 40–41 | `03 45` | **BCD 길이 → 345** |
| 42–386 | `30 30 30 32 …` | 바코드 데이터 345바이트 |
| 387 | `03` | ETX |
| 388 | `--` | LRC |

---

## 3. CMD 006 — 바코드 표시 응답 (PAD → TRM)

005 를 **수신하는 즉시** 회신합니다. 화면 렌더 성공 여부와 무관합니다.

```
"006" + FS + 결과코드(4)
```

| No | 필드명 | 크기 | 내용 |
| --- | --- | --- | --- |
| 1 | CMD | 3 | `006` |
| 2 | 결과코드 | 4 | `0000` = 성공 |

전체 프레임 (20바이트):

```
02 58 58 30 30 31 39 50 41 44 30 30 36 1C 30 30 30 30 03 74
│  └─XX─┘ └──0019──┘ └─PAD─┘ └─006─┘ FS └──0000──┘ │  └LRC
STX       길이=19(STX~ETX)                          ETX
```

> **미정**: `0000` 외 실패 코드 체계는 아직 정의되지 않았습니다. 현재 구현은 항상 `0000` 을
> 회신합니다. 렌더 실패(데이터 형식 오류 등)를 단말기에 알려야 한다면 코드 체계 협의가 필요합니다.

---

## 4. CMD 999 — 화면 미노출 요청 (TRM → PAD)

단말기가 "팜포인트가 띄운 화면을 걷어라" 고 지시하는 전문입니다.

```
"999"
```

**응답 전문이 없습니다.** 봉투 파싱 성공 시 나가는 ACK(`0x06 0x06 0x06`) 가 전부이며,
006 이나 010 같은 별도 회신은 보내지 않습니다.

### 4-1. 동작 범위 — 단말기 유래 화면만 닫습니다

이 플러그인은 오버레이가 아니라 화면 전환 구조라, "미노출" = 대기화면 복귀입니다.
다만 **CAT(POS) 가 띄운 화면과 결과 화면은 건드리지 않습니다.**

| 현재 화면 | 999 수신 시 | 판별 근거 |
| --- | --- | --- |
| `/barcode-display` | **닫음** | 005 는 단말기 전용 |
| `/point-earn-flow` | 컨텍스트가 TRM 유래일 때만 닫음 | 001 · 002 로 진입한 경우 |
| `/point-use-flow` | 컨텍스트가 TRM 유래일 때만 닫음 | 003 으로 진입한 경우 |
| 그 외 (대기화면 · 결과 · 가격표시기 · CAT 유래) | 유지 | — |

적립/사용 화면은 CAT 과 TRM 두 채널이 공용이라, 보관된 컨텍스트의 `source` 가
`PointUseSource.TERMINAL` 인지로 가릅니다. 닫을 때는 해당 화면의 sessionStorage 컨텍스트도
함께 폐기한 뒤 대기화면으로 이동합니다.

---

## 5. 바코드 인코딩 방식

### 5-1. 2차원 — QR Code

| 항목 | 값 |
| --- | --- |
| 규격 | QR Code (ISO/IEC 18004) |
| 라이브러리 | `qrcode-generator` v2.0.4 (MIT, 의존성 0개) |
| 인코딩 모드 | **Byte 모드** |
| 오류정정 레벨 | **M** (약 15% 복원) |
| 버전 | **자동 선택** (`typeNumber = 0`) — 데이터 길이에 맞는 최소 버전 |
| Quiet zone | 4모듈 (상하좌우 각 2모듈) |
| 출력 | SVG. 다크 모듈 전체를 `<path>` **하나로 병합** |

345바이트 EMVCo QR 결제 데이터 기준 **버전 14 (73×73 모듈)** 로 인코딩됩니다.

Byte 모드라 ASCII 데이터는 바이트가 그대로 들어갑니다(왕복 무손실 검증 완료). 다만
`qrcode-generator` 는 문자열을 UTF-8 로 바이트화하므로, **비-ASCII 바이트가 섞이면 원본과
달라질 수 있습니다.** 파서에서 비-ASCII 감지 시 경고 로그를 남깁니다.

> 모듈마다 `<rect>` 를 만들면 버전 14 기준 2,616개 노드가 생깁니다. `<path>` 하나로 합쳐
> DOM 부담을 없앴습니다.

### 5-2. 1차원 — EAN-13

| 항목 | 값 |
| --- | --- |
| 규격 | EAN-13 (GTIN-13) |
| 구현 | 자체 인코더 (`src/features/barcode/ean13.ts`) |
| 총 모듈 | **95** |
| 입력 | 12자리 또는 13자리 숫자 |

구조는 95모듈이며 다음과 같이 나뉩니다.

```
시작가드(101) + 좌측 6자리(42) + 중앙가드(01010) + 우측 6자리(42) + 종료가드(101)
```

- **첫 번째 자리는 막대로 그리지 않습니다.** 좌측 6자리(2~7번째)를 L / G 중 무엇으로
  인코딩할지 결정하는 **패리티 패턴**으로만 쓰이고, 사람이 읽는 숫자로만 표기됩니다
- 우측 6자리(8~13번째)는 전부 R 패턴
- L = 홀수 패리티, G = 짝수 패리티(R 을 뒤집은 값), R = L 의 보수

**체크digit** — 1~12번째 자리에 1, 3, 1, 3… 가중치를 곱해 합산한 뒤 `(10 - 합%10) % 10`.

- 12자리를 받으면 체크digit 을 **계산해 붙입니다**
- 13자리를 받으면 **검증만** 하고, 불일치해도 **수신값 그대로 렌더**한 뒤 경고 로그를 남깁니다
  (단말기가 보낸 값을 임의로 고치면 실제 상품과 다른 바코드가 표시되므로)
- 숫자가 아니거나 12/13자리가 아니면 렌더하지 않고 데이터를 텍스트로 표시합니다

### 5-3. 렌더 해상도 — 픽셀 정렬이 중요합니다

바코드는 **모듈 경계가 픽셀 경계에 정확히 떨어져야** 막대 굵기가 균일해집니다. 임의 폭으로
표시하면 모듈이 픽셀 사이에 걸려 막대가 2px / 3px 로 들쭉날쭉해지고 스캔율이 떨어집니다.

| | viewBox | 표시 크기 | 1모듈 |
| --- | --- | --- | --- |
| EAN-13 | 113 × 55 | **339 × 165px** | **3px** |
| QR (버전 14 기준) | 77 × 77 | 300 × 300px | 약 3.9px |

EAN-13 은 `EAN13_DISPLAY_WIDTH` 상수로 관리하며, 크기를 바꿀 때도 **viewBox 폭 113 의 정수배**
(226 / 339 / 452px …) 를 쓰면 됩니다.

가로 여백은 EAN-13 규격 최소값을 지킵니다 — **좌 11모듈 / 우 7모듈**.

사람이 읽는 숫자는 6자리를 한 덩어리로 그리지 않고 **각 자리를 자기 7모듈 그룹 중앙에 개별
배치**합니다. 덩어리 + `letter-spacing` 방식은 글자 폭이 그룹 폭(42모듈)을 넘겨 좌우 그룹이
겹쳤습니다.

---

## 6. 화면 동작

### 6-1. 표시 흐름

```
단말기 → 웹뷰 : 005 (종류 · timeout · 문구 · 바코드 데이터)
웹뷰          : ACK(06 06 06) 자동 회신
웹뷰 → 단말기 : 006 (0000)                        ← 수신 즉시
웹뷰          : 바코드 화면 진입 (#app 숨기고 자체 컨테이너 렌더)
        ↓
[timeout > 0]  N초 카운트다운 후 자동으로 대기화면 복귀
[timeout = 00] 자동 종료 없음 — 999 등 외부 신호를 기다림
        ↓
단말기 → 웹뷰 : 999 (선택)
웹뷰          : 바코드 화면 닫고 대기화면 복귀
```

### 6-2. 화면 구성

문구(상단) → 바코드(중앙) → 카운트다운(하단) 순으로 세로 중앙 정렬입니다.
`timeout = 00` 이면 카운트다운을 표시하지 않습니다.

### 6-3. 알려진 제약 — VAN 승인 화면과의 충돌

바코드 화면은 진입 시 SDK 렌더 컨테이너(`#app`)를 `display:none` 으로 숨깁니다. 이 상태에서는
**토스 VAN 모듈이 결제 승인 화면을 그리지 못할 수 있습니다.**

QR 결제 완료 후 단말기가 보내는 "승인 감사합니다" 전문이 화면에 표시되지 않는 사례가
관측되었습니다. 해당 전문은 TRM 봉투가 아니라 VAN 전문이므로 팜포인트가 파싱하지 않고
`sdk.van.write()` 로 그대로 중계하며, 화면 표시는 VAN 모듈 책임입니다.

원인 후보가 셋이며 아직 확정되지 않았습니다.

1. 바코드 화면의 `#app` 점유
2. 전문 내 2자리 코드 차이 (정상 표시되는 전문은 `"30"`, 미표시 전문은 `"05"`)
3. 미표시 전문 문구 중간의 NUL 바이트(`0x00`)

단말기가 QR 결제 완료 후 **999 를 보내주면** 바코드 화면이 닫히면서 `#app` 이 복구되어
1번은 해소됩니다.

---

## 7. 미정 항목

| 항목 | 현재 구현 | 필요한 협의 |
| --- | --- | --- |
| 006 실패 코드 | 항상 `0000` | `0000` 외 코드 체계 |
| timeout `00` 종료 | 자동 종료 없음 | 999 외 종료 수단이 있는지 |
| QR 결제 완료 후 999 | 미확인 | 단말기가 보내는지 확인 필요 |
| 1차원 데이터 형식 | EAN-13 (12/13자리) | 다른 자릿수·형식이 오는 경우 |

---

## 8. 관련 파일

| 파일 | 역할 |
| --- | --- |
| `src/pos/protocol/socket-constants.ts` | 005 / 006 / 999 커맨드 상수 |
| `src/pos/protocol/terminal-codec.ts` | `parseBarcodeDisplay()` · `makeBarcodeDisplayAck()` |
| `src/pos/socket-events.ts` | `TerminalBarcodeDisplay` · `TerminalHideScreen` |
| `src/pos/socket-gateway.ts` | 커맨드 → 이벤트 매핑, 005 전용 파싱 분기, 006 송신 |
| `src/features/app-session/app-session.service.ts` | 이벤트 → 화면 핸들러, 006 자동 회신 |
| `src/features/barcode/ean13.ts` | EAN-13 인코더 + SVG 렌더 |
| `src/pages/barcode-display.ts` | 바코드 표시 화면 (QR / EAN-13) |
| `src/main.ts` | 라우트 등록, 999 화면 종료 판별 |
