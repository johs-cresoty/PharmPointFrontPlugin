# 팜포인트 단말기(TERMINAL) 시리얼 통신 명세

> ### 📌 개정 안내 (재전달 사유)
> 앞서 전달드린 명세를 아래 3가지 보완하여 다시 전달드립니다. **필드 구성·값·의미는 변경 없으며**, 누락되었던 프레임 구조를 명시한 것이 핵심입니다.
>
> 1. **[중요] 프레임 구조(3-1) 추가** — Toss 리더기 모드에서는 **팜포인트 전문과 결제(VAN) 전문이 하나의 시리얼 포트로 함께 들어옵니다.** 플러그인은 전문 앞부분의 **`"XX"` 마커와 `"TRM"` 플래그**를 보고 둘을 구분해, 팜포인트 전문만 처리하고 나머지는 결제 모듈로 넘깁니다. 기존 명세에는 CMD 필드 표만 있고 이 봉투(envelope) 구조가 빠져 있어, 이대로 구현하면 **팜포인트 전문이 결제 전문으로 오인되어 동작하지 않습니다.** 이를 막기 위해 바이트 단위 구조와 검증된 실제 프레임 예시(hex)를 추가했습니다.
> 2. **ACK 문구 정리** — 플러그인이 ACK(`0x06 0x06 0x06`)를 회신한다는 사실을 고지로만 정리했습니다. (회신 여부 확인 요청은 불필요하여 삭제)
> 3. **`003` 응답 대기 규칙 변경** — 기존 "60초 타임아웃 권장"을 삭제했습니다. 플러그인이 완료 시 `004`, 무동작·이탈 시 `010`(INIT)을 **항상 회신**하므로 단말기 측 타임아웃이 필요 없습니다.

## 1. 개요
팜포인트 안드로이드 앱이 사용하던 단말기(TERMINAL) 통신을 Toss Place 플러그인 환경으로 이식합니다. 기존 안드로이드 프로토콜 그대로 유지하며, 통신 채널만 **TCP 소켓 → 물리 시리얼 포트** 로 변경됩니다.

**역할 분담**

| 주체 | 담당 |
| --- | --- |
| 단말기 | 시리얼로 `001` / `002` / `003` 전문 송신, `004` / `010` 응답 수신 |
| Toss 플러그인 | `sdk.serial.listen` 으로 수신·파싱·화면 표시, `sdk.serial.write` 로 `004` / `010` 송신 |

> **⚠️ 중요 — 전문 분기:** Toss 리더기 모드에서는 플러그인이 시리얼 수신 전문을 먼저 받아, **팜포인트 전문(플래그 `"TRM"`)만 플러그인이 처리**하고 **그 외 전문(KIS 등 결제 VAN 전문)은 Toss VAN 모듈로 전달**합니다. 따라서 단말기가 보내는 팜포인트 전문에는 **아래 3-1의 프레임 구조(특히 `"XX"` 마커와 `"TRM"` 플래그)가 반드시 포함**되어야 플러그인이 팜포인트 전문으로 인식합니다. (이 구조가 없으면 결제 VAN 전문으로 오인되어 처리되지 않습니다.)

---

## 2. 시리얼 물리 파라미터
Toss Place SDK `sdk.serial.open` 이 아래 스펙으로 포트를 여니, 단말기 측도 동일 설정 부탁드립니다.

| 항목 | 값 |
| --- | --- |
| baudRate | **9600** |
| dataBits | 8 |
| stopBits | 1 |
| parity | None |
| flow control | None |

> Toss SDK 는 `baudRate` 만 파라미터로 노출하고 나머지는 기본값(8N1)입니다. 필요 시 조정 가능하니 문제 있으면 회신 부탁드립니다.

---

## 3. 프레임 구조 및 커맨드 명세

### 3-1. 공통 프레임 구조 (Envelope) — **모든 전문 공통**
아래 3-2 / 3-3 의 커맨드 필드는 모두 이 봉투(envelope) 안에 담깁니다. 커맨드 표의 "1번 CMD" 는 이 구조의 **CMD** 영역을 가리킵니다.

```
STX │  "XX"  │  길이(4)  │  플래그(3)  │  CMD(3)  │  FS 필드 FS 필드 … FS 필드  │  ETX │ LRC
02  │ 58 58  │ ASCII×4   │ TRM / PAD   │ 001 등   │ 1C…                         │  03  │ 1B
```

| 구간 | 크기(byte) | 값 / 설명 |
| --- | --- | --- |
| STX | 1 | `0x02` — 전문 시작 |
| 마커 | 2 | `"XX"` = `0x58 0x58` (고정) |
| 길이 | 4 | **STX~ETX 까지의 총 바이트 수**(길이 필드 4바이트 자신 포함, **LRC 제외**), ASCII 숫자 4자리 zero-pad. 예: 34바이트 → `"0034"` |
| **플래그** | 3 | **단말기→플러그인 = `"TRM"`**(`0x54 0x52 0x4D`) / 플러그인→단말기 = `"PAD"`(`0x50 0x41 0x44`). **★ 팜포인트 전문 분기의 결정 기준** |
| CMD | 3 | `"001"`/`"002"`/`"003"`/`"004"`/`"010"` |
| 필드부 | 가변 | CMD 다음부터 ETX 전까지. **각 필드 앞에 FS(`0x1C`)** 를 붙여 구분 (CMD 와 첫 데이터 필드 사이에도 FS 1개) |
| ETX | 1 | `0x03` — 전문 끝 |
| LRC | 1 | 체크값. `STX 다음(마커)부터 ETX 까지` 전 바이트 XOR 누적 후 `| 0x20`. (즉 `LRC = (XOR(data[1..ETX])) | 0x20`) |

**핵심 규칙**
- 플래그가 **`"TRM"` 이 아니면** 플러그인은 팜포인트 전문으로 처리하지 않습니다(결제 VAN 전문으로 전달). 단말기 송신 전문에 **반드시 `"XX"` 마커 + `"TRM"` 플래그를 포함**하세요.
- 필드부는 **FS(`0x1C`)로 시작**하여(=CMD 바로 뒤 FS) 각 필드를 FS로 구분합니다.

#### ✅ 검증된 예시 프레임 — `003` 포인트 사용 요청
필드값 예: 사업자번호 `2018182695`, (미사용) `0`, OTC `5000`, VAT `0`

```
02 58 58 30 30 33 34 54 52 4D 30 30 33 1C 32 30 31 38 31 38 32 36 39 35 1C 30 1C 35 30 30 30 1C 30 03 73
```

| 바이트 | 의미 |
| --- | --- |
| `02` | STX |
| `58 58` | 마커 "XX" |
| `30 30 33 34` | 길이 "0034" (=STX~ETX 34바이트) |
| `54 52 4D` | 플래그 "TRM" |
| `30 30 33` | CMD "003" |
| `1C 32 30 31 38 31 38 32 36 39 35` | FS + "2018182695" (사업자번호) |
| `1C 30` | FS + "0" (미사용) |
| `1C 35 30 30 30` | FS + "5000" (OTC) |
| `1C 30` | FS + "0" (VAT) |
| `03` | ETX |
| `73` | LRC |

> 위 프레임은 실제 플러그인 파서로 역검증했습니다 → `flag="TRM"`, `cmd="003"`, OTC=`5000`, VAT=`0` 정상 인식.

---

### 3-2. 단말기 → 플러그인

각 표의 **1번 CMD** 는 3-1 envelope 의 CMD 영역, 나머지 필드는 CMD 뒤 FS 구분 필드입니다.

#### CMD 001 — 포인트 적립 (단일 결제)
| No | 필드명 | 내용 |
| --- | --- | --- |
| 1 | CMD | `001` |
| 2 | 거래일자 | `yyyyMMdd` (8자리) |
| 3 | 승인번호 |  |
| 4 | 결제수단 | 신용 `C` / 현영 `H` / 연동현영 `B` / 현금수납 `M` 등 |
| 5 | ETC |  |
| 6 | OTC |  |
| 7 | VAT |  |
| 8 | 포인트사용여부 | `0: false` / `1: true` (포인트 사용 후 결제 여부) |

#### CMD 002 — 포인트 적립 (복합 결제)
| No | 필드명 | 내용 |
| --- | --- | --- |
| 1 | CMD | `002` |
| 2 | 거래일자 | `yyyyMMddhhmmss` (14자리) |
| 3 | 승인번호 |  |
| 4 | 결제수단 | 신용 `C` / 현영 `H` / 연동현영 `B` / 현금수납 `M` 등 |
| 5 | ETC |  |
| 6 | OTC |  |
| 7 | VAT |  |
| 8 | 승인번호 |  |
| 9 | 결제수단 |  |
| 10 | ETC |  |
| 11 | OTC |  |
| 12 | VAT |  |

> 복합결제는 포인트 사용 불가하여 사용 후 결제 여부는 판단하지 않음

#### CMD 003 — 포인트 사용 요청
| No | 필드명 | 내용 | 예시 값 (실 로그) |
| --- | --- | --- | --- |
| 1 | CMD | `003` | `003` |
| 2 | 사업자번호 | 10자리 사업자등록번호 | `2018182695` |
| 3 | (미사용) | 안드로이드에서 파싱하지 않는 자리. 실 로그 관측값 `0` — 담당자 스펙 유지 | `0` |
| 4 | OTC | 공급가 | `5000` |
| 5 | VAT | 부가세 | `0` |

> **참고**: 실기기 로그로 검증된 필드 배치입니다. 3번 필드는 플러그인이 사용하지 않으나 프레임 정합성을 위해 기존 스펙 그대로 채워 송신 부탁드립니다 (`0` 관측). 바이트 예시는 3-1 검증 프레임 참고.

### 3-3. 플러그인 → 단말기

#### CMD 004 — 포인트 사용 응답
| No | 필드명 | 내용 |
| --- | --- | --- |
| 1 | CMD | `004` |
| 2 | 고객전화번호 | 예: `01012345678` |
| 3 | 현재포인트 | 사용 후 잔액 |
| 4 | 사용포인트 | 방금 사용한 포인트 |

> 플러그인 송신 프레임 예시(참고): 전화 `01012345678`, 잔액 `1500`, 사용 `500`
> `02 58 58 30 30 33 35 50 41 44 30 30 34 1C 30 31 30 31 32 33 34 35 36 37 38 1C 31 35 30 30 1C 35 30 30 03 70`
> (플래그 `"PAD"` = `50 41 44` 주의 — 송신은 PAD, 수신은 TRM)

#### CMD 010 — 취소 (INIT)
| No | 필드명 | 내용 |
| --- | --- | --- |
| 1 | CMD | `010` |
| 2 | 메시지 | `INIT` |

---

## 4. 통신 흐름
```
[대기 상태]
  플러그인이 sdk.serial.open({ baudRate: 9600, intercept: true }) 으로 포트 open 후 sdk.serial.listen 등록
  단말기는 결제 완료 대기 상태
        ↓
[포인트 적립 흐름]
  단말기 → 플러그인 : 001 또는 002 (결제 데이터)
  플러그인 : 화면 표시 → 사용자 휴대폰 번호 입력 → 서버 API 로 적립 처리
  (별도 응답 전문 없음. 화면에서 완료)
        ↓
[포인트 사용 흐름]
  단말기 → 플러그인 : 003 (사용 요청)
  플러그인 : 화면 표시 → 사용자 포인트 입력 → 서버 API 로 사용 처리
  플러그인 → 단말기 : 004 (사용 결과 회신)
        ↓
[취소 흐름]
  사용자 뒤로가기 · 이탈 시
  플러그인 → 단말기 : 010 (INIT)
```

---

## 5. 개발 시 유의 사항
1. **프레임 봉투 필수** — 모든 팜포인트 전문은 3-1 구조(`STX + "XX" + 길이 + "TRM" + CMD + FS필드… + ETX + LRC`)를 지켜야 합니다. 특히 `"XX"` 마커와 `"TRM"` 플래그가 없으면 플러그인이 팜포인트 전문으로 인식하지 못합니다.
2. **문자 인코딩** — payload 는 EUC-KR. 실제 필드가 숫자·ASCII 만이면 UTF-8 과 결과 동일.
3. **길이 계산** — 4자리 길이는 **STX 부터 ETX 까지**를 카운트하며 길이 필드 4바이트 자신을 포함하고 **LRC 는 제외**합니다.
4. **LRC** — `STX 다음 바이트부터 ETX 까지` XOR 누적한 값에 `| 0x20`.
5. **ACK** — 플러그인은 유효 전문 수신 시 ACK(`0x06 0x06 0x06`)를 자동 회신합니다. 단말기 측에서 처리(또는 무시) 바랍니다.
6. **003 응답** — `003` 송신 후 플러그인은 처리 완료 시 `004`, 무동작·이탈로 취소될 시 `010`(INIT)을 반드시 회신합니다. 단말기 자체 타임아웃은 두지 않아도 됩니다.
7. **테스트 지원** — 개발 중 단말기 없이 통신 테스트할 수 있는 방법(시뮬레이터 등)이 있으면 공유 부탁드립니다.

---
*Toss 관련 문서: https://docs.tossplace.com/reference/plugin-sdk/front/serial.html*
